# Generated from orogen/lib/orogen/templates/tasks/CMakeLists.txt

include(water_probe_acquanativa_ap3TaskLib)
ADD_LIBRARY(${WATER_PROBE_ACQUANATIVA_AP3_TASKLIB_NAME} SHARED
    ${WATER_PROBE_ACQUANATIVA_AP3_TASKLIB_SOURCES})
add_dependencies(${WATER_PROBE_ACQUANATIVA_AP3_TASKLIB_NAME}
    regen-typekit)


TARGET_LINK_LIBRARIES(${WATER_PROBE_ACQUANATIVA_AP3_TASKLIB_NAME}
    ${OrocosRTT_LIBRARIES}
    ${QT_LIBRARIES}
    ${WATER_PROBE_ACQUANATIVA_AP3_TASKLIB_DEPENDENT_LIBRARIES})
SET_TARGET_PROPERTIES(${WATER_PROBE_ACQUANATIVA_AP3_TASKLIB_NAME}
    PROPERTIES LINK_INTERFACE_LIBRARIES "${WATER_PROBE_ACQUANATIVA_AP3_TASKLIB_INTERFACE_LIBRARIES}")
SET_TARGET_PROPERTIES(${WATER_PROBE_ACQUANATIVA_AP3_TASKLIB_NAME}
    PROPERTIES INTERFACE_LINK_LIBRARIES "${WATER_PROBE_ACQUANATIVA_AP3_TASKLIB_INTERFACE_LIBRARIES}")

INSTALL(TARGETS ${WATER_PROBE_ACQUANATIVA_AP3_TASKLIB_NAME}
    RUNTIME DESTINATION bin
    LIBRARY DESTINATION lib/orocos)

INSTALL(FILES ${WATER_PROBE_ACQUANATIVA_AP3_TASKLIB_HEADERS}
    DESTINATION include/orocos/water_probe_acquanativa_ap3)

